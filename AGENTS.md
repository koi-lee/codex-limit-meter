# Codex Limit Meter 项目协作规则

- 本项目是独立的 macOS Swift 产品，不复制星岸 AI 主站或其他产品源码。
- 修改前先读取本文件和 `README.md`；涉及发布时同时读取 `TESTING.md` 和 `DEPLOYMENT.md`。
- 用户可见功能、版本、下载地址或系统要求变化时，检查星岸 AI 主站对应产品介绍页是否需要同步。
- 不读取、提交或输出真实密钥、证书、签名材料和用户隐私数据。
- 未经用户明确授权，不自动 commit、push、创建 Release、签名发布或触发线上部署。
- 构建缓存和发布产物只保留本地，不提交 `.build/`、`dist/` 或 `.env`。

